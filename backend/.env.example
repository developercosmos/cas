PORT=4000
HOST=0.0.0.0
NODE_ENV=development
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRY=7d
# CORS_ORIGIN: Use * for all origins in dev, or comma-separated list in production
CORS_ORIGIN=*
