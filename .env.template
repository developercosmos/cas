# CAS Platform Environment Configuration Template
# Copy this file to .env and update with your values

# ===== Database Configuration =====
POSTGRES_USER=dashboard_user
POSTGRES_PASSWORD=dashboard_password
POSTGRES_DB=dashboard_db

# ===== Application Configuration =====
PORT=4000
NODE_ENV=development
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
CORS_ORIGIN=http://localhost:3000

# ===== AI Provider Configuration =====

# Ollama (Local AI - Primary Provider)
# Default: http://ollama:11434 (Docker internal)
# For host access: http://localhost:11434
OLLAMA_BASE_URL=http://ollama:11434

# OpenAI (Cloud AI - Fallback Provider)
# Get your API key from: https://platform.openai.com/api-keys
# Leave empty to disable OpenAI fallback
OPENAI_API_KEY=

# Google Gemini (Cloud AI - Backup Provider)
# Get your API key from: https://makersuite.google.com/app/apikey
# Leave empty to disable Gemini fallback
GEMINI_API_KEY=

# ===== RAG Plugin Configuration =====

# OpenAI Models
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_CHAT_MODEL=gpt-3.5-turbo

# RAG Processing
RAG_MAX_CHUNKS=1000
RAG_CHUNK_OVERLAP=200
RAG_MAX_CONTEXT_LENGTH=4000
RAG_DEFAULT_TEMPERATURE=0.7
RAG_DEFAULT_RETRIEVAL_COUNT=5

# ===== LDAP Configuration (Optional) =====
LDAP_URL=ldap://your-domain-controller:389
LDAP_BIND_DN=CN=Service Account,OU=Users,DC=domain,DC=com
LDAP_BIND_PASSWORD=your-ldap-password
LDAP_SEARCH_BASE=DC=domain,DC=com
LDAP_USER_FILTER=(sAMAccountName={{username}})

# ===== Notes =====
# 
# Multilingual Support:
# - Ollama supports 100+ languages including English and Bahasa Indonesia
# - Models: llama3.2, mistral (good for Indonesian)
# - Embeddings: nomic-embed-text (multilingual)
#
# Fallback Chain:
# 1. Ollama (free, local, fast)
# 2. OpenAI (requires API key, high quality)
# 3. Gemini (requires API key, alternative)
#
# Setup Instructions:
# 1. Copy this file: cp .env.template .env
# 2. Update values above
# 3. Start services: docker-compose up -d
# 4. Pull Ollama models: docker exec cas_ollama ollama pull llama3.2
